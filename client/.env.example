# --- Client configuration ---
# Where the Vite dev server proxies /api and /uploads requests.
VITE_API_TARGET=http://localhost:5000

# Port for the Vite dev server. Keep this in sync with CLIENT_ORIGIN in server/.env.
VITE_PORT=5173
